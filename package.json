{
  "name": "nexum",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.4.1",
    "pg": "^8.18.0"
  },
  "devDependencies": {
    "@types/pg": "^8.16.0"
  }
}
